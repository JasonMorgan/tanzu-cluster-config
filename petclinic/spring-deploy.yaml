---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: petclinic
  name: petclinic-deployment
  labels:
    app: petclinic
spec:
  replicas: 3
  selector:
    matchLabels:
      app: petclinic
  template:
    metadata:
      labels:
        app: petclinic
    spec:
      containers:
      - name: petclinic
        imagePullPolicy: Always
        image: harbor.59s.io/59s/spring-petclinic@sha256:0a0d02b38c949af0defca6c863f0307937db4375aa02249e2691aa5224319d41
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: "400Mi"
            cpu: "100m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 3
          periodSeconds: 6
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 6
        startupProbe:
          httpGet:
            path: /
            port: 8080
          failureThreshold: 30
          periodSeconds: 10
        # env: 
        #   - name: spring_profiles_active
        #     value: mysql
        #   - name: MYSQL_URL
        #     value: "jdbc:mysql://pet-db-mysql.default.svc.cluster.local/petclinic"
---
apiVersion: v1
kind: Service
metadata:
  namespace: petclinic
  name: pb-frontend
spec:
  selector:
    app: petclinic
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
      ingress.kubernetes.io/force-ssl-redirect: "true"
      kubernetes.io/ingress.class: contour
      kubernetes.io/tls-acme: "true"
  name: ingress-petclinic
  namespace: petclinic
spec:
    rules:
    - host: petclinic.59s.io
      http:
        paths:
        - backend:
            serviceName: pb-frontend
            servicePort: 80
    tls:
    - hosts:
      - petclinic.59s.io
      secretName: petclinic